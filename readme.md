# OSPretty

### An attempt at tidying up all of the crufty python2/3 ways of  using the Operating System

This attempt will primarily try to align to the following paradigms:

- Use of the code should be no longer than the corresponding operating system version
- Defaults should always be sane
- Always try to `re:use`
- Sometime `re:use` isn't the best way